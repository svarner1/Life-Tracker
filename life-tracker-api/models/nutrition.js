const db = require("../db")

class ExerciseData {
    static async listNutritionDataForUser({id, nutrition_entry_id}){
       //will return a list of all the 
    }

    static async createNutritionEntry({ users_id, calories }){
        //method will take a user's order and store it in the database
    }
}

module.exports = ExerciseData